# Cocos Client Development Plan

> 基于 `DEV_PLAN.md` 需求文档，并结合 `client-cocos` 当前代码结构与已实现能力整理。本文档用于指导 Cocos Creator 客户端后续开发、拆分任务和安排阶段交付。

---

## 1. 文档目标

本文档聚焦 `client-cocos` 的客户端开发计划，目标如下：

- 识别当前代码库已具备的能力边界
- 对照 `DEV_PLAN.md` 明确缺口与改造重点
- 制定按阶段推进的客户端开发路线
- 给出模块级拆解、优先级、排期与主要风险

---

## 2. 当前代码库现状

### 2.1 已完成能力

结合现有代码，当前 Cocos 客户端已具备以下基础能力：

- 启动加载流程
  - `assets/Scripts/Game/Load.ts`
  - 启动时拉取远端 `config.json`
  - 预加载登录、大厅、弹窗、设置、商城等基础 Bundle

- 客户端主容器
  - `assets/Scripts/Game/Client.ts`
  - 已管理登录层、大厅层、游戏加载层、游戏大厅层、游戏房间层、提示层、全局 BGM

- 登录注册与账号态
  - `assets/Scripts/Game/Login.ts`
  - 已支持登录、注册、验证码、自动读取本地账号信息、获取玩家资料

- 大厅与基础功能
  - `assets/Scripts/Game/Hall.ts`
  - `assets/Scripts/Game/GameList.ts`
  - 已支持玩家资料展示、头像拉取、游戏列表入口、个人中心、银行、商城、设置、邮件、客服入口

- 资源 Bundle 加载
  - `assets/Scripts/Manager/ResourceLoader.ts`
  - 已支持按 Bundle + 资源清单逐项加载

- HTTP REST 能力
  - `assets/Scripts/Manager/GameManager.ts`
  - 已支持普通 GET/POST、鉴权 GET/POST、玩家信息管理、登录态维护、消息签名

- WebSocket 长连接能力
  - `assets/Scripts/Manager/NetworkManager.ts`
  - 已支持连接、断线重连、心跳、进入场地、消息发送、连接回调通知

- 语音接收播放
  - `assets/Scripts/Manager/VoiceManager.ts`
  - 当前仅支持接收并播放语音消息，不支持发送语音

- 掼蛋完整链路样板
  - `assets/Scripts/Game/GameLoader.ts`
  - `assets/Scripts/Game/GuanDan/GuanDanHall.ts`
  - `assets/Scripts/Game/GuanDan/GuanDanRoom.ts`
  - 已打通：游戏加载 -> 游戏大厅 -> 创建/加入房间 -> 场地进入 -> 牌桌同步 -> 出牌 -> 结算 -> 解散 -> 重连同步

### 2.2 当前架构特征

当前项目本质上是：

- 一个中心化客户端壳层
- 一个通用大厅
- 一个通用网络层
- 一个资源按 Bundle 加载的基础设施
- 一个“掼蛋”专有实现作为完整游戏样板

这说明项目已经有可复用基础，但尚未进入“多游戏平台化”的阶段。

### 2.3 当前明显限制

当前实现距离 `DEV_PLAN.md` 目标仍有明显差距：

- 仅有掼蛋完整实现，六款新游戏尚未接入
- 未实现统一横屏适配与安全区系统
- 未实现版本检测、热更新、资源版本校验
- 未实现统一特效系统与性能分级
- 未实现震动反馈体系
- 未实现崩溃日志与性能上报
- 设置项只覆盖音乐/音效，未覆盖语音、震动、特效档位、低性能模式
- 游戏代码复用层不足，掼蛋逻辑耦合较重，不适合直接横向复制六次

---

## 3. 与 DEV_PLAN.md 的差距分析

### 3.1 横屏基础框架差距

`DEV_PLAN.md` 要求：

- 设计基准为 `1920x1080`
- 适配 16:9 到 21:9
- 处理安全区、刘海屏、挖孔屏、平板
- 横屏桌面区域需统一布局

当前现状：

- `settings/v2/packages/project.json` 仍为 `1600x900`
- 代码中未发现安全区、横屏旋转、屏幕恢复重排等逻辑
- 未抽象 `AppLayoutAdapter`

结论：

- 横屏适配系统必须作为第一优先级平台任务单独建设

### 3.2 版本更新系统差距

`DEV_PLAN.md` 要求：

- 启动时版本检测
- 强更/热更/无更新分支处理
- 游戏进入前资源版本检查
- 更新包校验与失败处理

当前现状：

- `Load.ts` 仅拉配置并加载基础资源
- `ResourceLoader.ts` 仅负责本地 Bundle 加载
- 未发现版本清单、热更新、校验、失败回滚实现

结论：

- 需要单独增加 `VersionUpdateManager` 和资源版本管理机制

### 3.3 多游戏框架差距

`DEV_PLAN.md` 要求每款游戏具备：

- 入口卡片
- 专属加载页
- 创建房间
- 等待房间
- 横屏牌桌
- 单局结算
- 总结算
- 回放

当前现状：

- 掼蛋具备完整闭环
- 其余游戏尚未接入
- 游戏加载器 `GameLoader.ts` 只处理 `GuanDan`

结论：

- 必须从掼蛋样板中提炼通用游戏模板，而不是逐游戏复制

### 3.4 音效与设置差距

`DEV_PLAN.md` 要求：

- 多音轨、预加载、淡入淡出、音效池化
- 三类游戏音效体系
- 玩家设置扩展：震动、语音播报、特效强度、低性能模式

当前现状：

- 已有 BGM 和部分音效控制能力
- 掼蛋存在专有 `AudioControl`
- `DlgSetting.ts` 只支持音乐/音效音量与静音

结论：

- 需要抽一层全局音频管理器与完整设置中心

### 3.5 网络与协议差距

`DEV_PLAN.md` 中新增接口采用：

- `/api/...` 风格 HTTP
- `room.join / game.action / room.update` 风格 WS 事件

当前代码采用：

- `/player/...` 风格 HTTP
- `MsgXXX` 风格 WS 消息

结论：

- 客户端必须增加协议适配层，兼容当前服务端与后续新协议演进

### 3.6 质量体系差距

当前项目缺少：

- 崩溃日志采集
- 资源加载失败上报
- 热更新失败上报
- FPS/内存/耗时监控
- 低端机自动降级

结论：

- 这些能力应放在第三阶段统一补齐

---

## 4. 核心开发原则

为避免后续六款游戏开发失控，建议遵循以下原则：

### 4.1 平台层先于游戏层

优先建设通用框架，再接入具体游戏，避免每个游戏重复实现：

- 横屏适配
- 资源与版本管理
- 网络协议适配
- 通用房间生命周期
- 通用玩家位与倒计时
- 通用聊天/表情/语音/托管/解散
- 通用结算结构

### 4.2 先抽模板再批量接入

建议优先抽两套模板：

- `MahjongRoomBase`
- `PokerRoomBase`

字牌类 `ZipaiRoomBase` 建议与「益阳歪胡子」同一周期落地，避免字牌类能力长期悬空导致后续返工。

### 4.3 六款游戏一次性实现（并行工作流）

本项目按 `DEV_PLAN.md` 要求，需要一次性实现全部新增游戏：

- 桃江麻将、红中麻将、长沙麻将
- 跑得快、沅江千分
- 益阳歪胡子

实现策略采用“并行开发 + 流水线联调”的方式：

- **并行开发**：六款游戏的资源、UI、规则、牌桌逻辑同步推进
- **流水线联调**：在统一平台层与模板就绪后，按“可联调最早的游戏”先打通端到端链路，再把通用问题回灌模板，避免同类缺陷在六条线重复出现
- **统一底座前置**：横屏适配、资源/版本、网络协议适配、通用房间生命周期、通用结算/回放壳层必须先落地，否则六条并行线会被大量基础问题阻塞

### 4.4 协议走兼容路线

在服务端完全稳定之前，客户端不要直接强依赖新 `/api/...` 和新 WS 事件格式，建议通过适配层兼容：

- 当前 `/player/...`
- 后续 `/api/...`
- 当前 `MsgXXX`
- 后续事件化消息

---

## 5. 总体开发路线

建议分为五个阶段推进。

### 阶段 0：方案冻结与技术基线

目标：

- 统一需求理解
- 冻结客户端技术基线
- 明确服务端协议与资源规范

主要任务：

- 确认最终设计分辨率为 `1920x1080`
- 与服务端确认新旧接口兼容策略
- 输出客户端目录规范、命名规范、Bundle 规范
- 输出 UI 规范：横屏牌桌、安全区、按钮热区、状态栏布局
- 输出日志与埋点规范

交付物：

- 客户端技术方案文档
- 协议适配策略文档
- 资源组织规范

### 阶段 1：平台层建设

目标：

- 把当前“单游戏客户端”升级为“多游戏平台客户端”

主要任务：

- 横屏适配系统
  - 新建 `AppLayoutAdapter`
  - 支持分辨率、安全区、横屏旋转、后台恢复

- 客户端状态机与全局壳层
  - 统一管理 `Boot / Auth / Lobby / LoadingGame / InGame / Updating`
  - 增加顶部状态栏、全局 loading、断网提示

- 资源与版本基础设施
  - 新增版本检测流程
  - 新增资源版本清单
  - 新增进入游戏前资源校验

- 网络协议适配层
  - 将 HTTP 接口封装为统一服务
  - 将 `MsgXXX` 与事件化 WS 协议隔离

- 设置中心扩展
  - 震动开关
  - 语音播报开关
  - 特效强度
  - 低性能模式

交付物：

- 可复用的平台层框架
- 多游戏接入基础能力

### 阶段 2：游戏模板抽象

目标：

- 从掼蛋样板中提炼通用游戏框架

主要任务：

- 抽象通用游戏入口与加载页流程
- 抽象创建房间/加入房间/等待房间流程
- 抽象通用牌桌基础能力
  - 玩家位
  - 托管
  - 解散投票
  - 聊天/表情/语音
  - 倒计时
  - 断线重连
  - 单局结算/总结算

- 抽象两套基础模板
  - `MahjongRoomBase`
  - `PokerRoomBase`

- 视情况将掼蛋回归到统一模板体系，验证抽象质量

交付物：

- 房间模板基类
- 游戏大厅模板
- 结算模板

### 阶段 3：六款游戏并行落地

目标：

- 在统一框架下，同时接入 `DEV_PLAN.md` 全部新增游戏，并保证每款游戏满足“入口卡片 → 专属加载页 → 创建房间 → 等待房间 → 横屏牌桌 → 单局结算 → 总结算 → 回放”

#### 3.1 桃江麻将

重点：

- 2 人玩法
- 横屏等待房间
- 麻将桌面与牌墙基础能力
- 吃/碰/杠/胡/过/托管/解散
- 赖子牌标识
- 房费方式选择
- 单局结算/总结算

价值：

- 麻将类基线能力落地（牌桌/牌墙/吃碰杠胡/赖子/结算）

#### 3.2 红中麻将

重点：

- 红中赖子玩法
- 听牌提示面板
- 胡牌牌型展示
- 多赖子最优组合提示

价值：

- 麻将变体能力落地（红中/赖子、听牌提示、牌型展示、多赖子组合）

#### 3.3 跑得快

重点：

- 扑克牌桌布局
- 手牌扇形展开与选牌交互
- 牌型判断结果展示
- 非法牌型提示
- 托管模式
- 断线重连恢复

价值：

- 扑克类基线能力落地（手牌交互/牌型/托管/重连/结算）

#### 3.4 长沙麻将

重点：

- 起手胡检测弹窗（缺一色/板板胡/大四喜/六六顺/节节高/三同/一枝花）
- 中鸟动画与结算展示
- 庄家标识、番数展示

#### 3.5 益阳歪胡子

重点：

- 字牌竖排展示与分组
- 吃/碰/偎/跑/提/胡/过操作链路
- 胡息面板实时更新
- 吃牌多组合选择弹窗

#### 3.6 沅江千分

重点：

- 3-5 人可配置
- 叫分/抢庄阶段 UI
- 计分牌高亮（5/10/K）
- 累计积分面板与目标分达标提示

交付物：

- 六款游戏均可联调、可验收版本（满足各自完整闭环）

### 阶段 4：音效/特效/回放/上报全量化

目标：

- 在六款游戏均可跑通后，补齐 `DEV_PLAN.md` 的“全局能力与体验指标”，并把共性素材（音效、特效、回放、上报、震动）做成可配置体系

### 阶段 5：性能、发布与打磨

目标：

- 提升稳定性、可运营性与发布能力

主要任务：

- 热更新与强制更新
- 性能监控与自动降级
- 崩溃日志与资源异常上报
- 回放页面
- 震动反馈
- 地方语音包
- 特效 L1-L5 与特效强度档位

交付物：

- 可上线版本
- 验收与发布文档

---

## 6. 模块级开发拆解

### 6.1 客户端基础框架

建议新增或重构模块：

- `AppLayoutAdapter`
  - 屏幕尺寸与安全区计算
  - UI 适配参数输出

- `SceneFlowManager`
  - 管理全局页面状态切换

- `VersionUpdateManager`
  - 版本检测
  - 热更新
  - 更新失败处理

- `PerformanceManager`
  - FPS 监控
  - 性能档位
  - 自动降级

### 6.2 资源系统

在现有 `ResourceLoader.ts` 基础上升级：

- 支持资源 manifest
- 支持版本号校验
- 支持失败重试
- 支持进入游戏前增量加载
- 支持下载进度与错误回调

### 6.3 网络与协议

建议新增统一封装层：

- `ApiService`
  - 封装 HTTP 接口
  - 兼容旧 `/player/...` 和新 `/api/...`

- `WsEventRouter`
  - 屏蔽底层消息差异
  - 提供统一房间事件分发

- `ProtoAdapter`
  - 负责 `MsgXXX` 与业务事件映射

### 6.4 通用游戏框架

建议抽象：

- `GameEntryBase`
- `GameHallBase`
- `RoomBase`
- `SeatView`
- `CountdownController`
- `ChatPanel`
- `VoiceIndicator`
- `DissolveVotePanel`
- `SettlementPanelBase`

### 6.5 音频系统

建议平台化：

- `AudioManager`
  - BGM
  - 环境音
  - SFX
  - 语音播报
  - 语音消息播放队列

- 特性要求
  - 预加载
  - 多音轨
  - 淡入淡出
  - 音量层级
  - 后台切换暂停/恢复

### 6.6 设置系统

在现有 `DlgSetting.ts` 基础上扩展：

- 音效开关
- BGM 开关
- 语音播报开关
- 震动开关
- 特效强度选择
- 低性能模式

### 6.7 日志与上报

建议新增：

- `LogReporter`
- `CrashCollector`
- `ResourceReportService`

覆盖场景：

- 崩溃日志
- 资源加载失败
- 更新失败
- 场景进入失败
- WS 重连失败
- 结算异常

---

## 7. 建议目录结构

建议后续逐步整理为如下结构：

```text
assets/
  Scripts/
    App/
      AppLayoutAdapter.ts
      SceneFlowManager.ts
      VersionUpdateManager.ts
      PerformanceManager.ts
    Network/
      ApiService.ts
      WsEventRouter.ts
      ProtoAdapter.ts
    GameCommon/
      GameEntryBase.ts
      GameHallBase.ts
      RoomBase.ts
      SeatView.ts
      ChatPanel.ts
      DissolveVotePanel.ts
      SettlementPanelBase.ts
    Games/
      TaoJiangMahjong/
      HongZhongMahjong/
      PaoDeKuai/
      ChangShaMahjong/
      YiYangWaiHuZi/
      YuanJiangQianFen/
  resources/
    app-common/
    games/
    hotfix/
    version-manifest/
```

说明：

- 短期内不必一次性重构完全部旧目录
- 但新增游戏建议直接遵循新目录结构，避免历史结构继续扩散

---

## 8. 优先级建议

### P0

- 横屏适配系统
- 全局客户端状态机
- 资源版本检测与游戏前校验
- 协议适配层
- 通用房间模板
- 桃江麻将
- 红中麻将
- 跑得快

### P1

- 长沙麻将
- 沅江千分
- 完整音效系统
- 特效档位与部分特效系统
- 设置中心扩展

### P2

- 益阳歪胡子深度打磨
- 崩溃与性能上报
- 回放页面
- 震动反馈
- 地方语音包
- 全量特效系统

---

## 9. 风险评估

### 9.1 协议风险

风险：

- `DEV_PLAN.md` 中的接口模型与当前实现不一致

应对：

- 增加适配层
- 先兼容旧协议
- 新协议逐步切换

### 9.2 架构风险

风险：

- 若直接复制掼蛋代码开发新游戏，会形成多个超大脚本，后续维护成本极高

应对：

- 平台层先行
- 模板先行
- 新游戏必须基于模板接入

### 9.3 资源风险

风险：

- 六款新游戏资源量大，美术、音频、特效可能成为主瓶颈

应对：

- 提前定义 Bundle 粒度
- 提前锁定资源规范
- 先做可联调灰盒资源，再逐步替换正式资源

### 9.4 真机适配风险

风险：

- 横屏安全区问题容易在后期集中暴露

应对：

- 第一阶段就引入真机验证
- 覆盖刘海屏、挖孔屏、平板和超宽屏设备

### 9.5 性能风险

风险：

- 特效、牌桌动画、音频并发后低端 Android 可能掉帧

应对：

- 早期加入性能监控
- 尽早定义低中高档特效策略

---

## 10. 建议排期

以下为建议排期，可根据人力调整。

### 第 1 周

- 需求对齐
- 协议梳理
- 客户端技术方案冻结
- UI 横屏规范冻结

### 第 2-3 周

- 横屏适配系统
- 客户端状态机
- 设置中心扩展
- 资源/版本底座
- 网络协议适配层

### 第 4-5 周

- 抽象 `MahjongRoomBase`
- 抽象 `PokerRoomBase`
- 提炼通用等待房间、结算、聊天、托管、解散、重连

### 第 6-8 周

- 六款游戏并行开发（UI/资源/规则/房间链路）
- 平台层/模板层问题集中修复并回灌

### 第 9-11 周

- 六款游戏并行联调与补齐（断线重连、结算、回放壳层、设置联动）

### 第 12-13 周

- 音效系统完善
- 特效系统接入
- 性能优化
- 日志上报
- 回放与震动

### 第 14 周

- 全量回归
- 多机型测试
- 验收修复
- 发布演练

---

## 11. 推荐实施顺序

在“六款游戏一次性实现”的目标下，推荐采用如下推进方式（兼容并行开发）：

1. 先落地平台层与协议适配层（否则六条线会被横屏/网络/资源/版本等共性问题阻塞）
2. 再落地通用房间模板（麻将/扑克/字牌三条模板线至少提供可运行骨架）
3. 六款游戏并行开发与联调（采用流水线联调：先打通端到端的可联调游戏链路，再把问题回灌到模板层）
4. 全量补齐热更新、性能、日志、回放、震动、音效与特效配置

---

## 12. 阶段验收标准

### 平台层验收

- 支持横屏 1920x1080 设计基准
- 支持 16:9 到 21:9 自适配
- 支持安全区与横屏恢复
- 支持统一资源校验与更新入口
- 支持断线重连和统一错误提示

### 游戏验收（六款）

- 桃江麻将、红中麻将、长沙麻将、跑得快、益阳歪胡子、沅江千分均具备独立入口、加载页、牌桌、单局结算、总结算、回放入口
- 支持创建房间、加入房间、等待房间
- 支持断线重连恢复
- 支持音效设置和基础音效

### 全量验收

- 六款游戏全部具备独立入口和完整闭环
- 热更新、强更和资源版本检查可用
- 音效、特效、震动支持独立控制
- 低端 Android 可维持最低目标帧率

---

## 13. 最终结论

当前 `client-cocos` 已经具备：

- 登录/大厅底座
- 资源 Bundle 加载底座
- HTTP/WS 网络底座
- 掼蛋完整样板

因此它不是从零开始，而是一个已经完成“第一款游戏验证”的客户端工程。

后续正确的推进方式不是直接堆六款游戏，而是：

- 先把项目升级为多游戏平台化客户端
- 再通过模板化方式批量接入新游戏

建议的主路径为：

`平台层改造 -> 房间模板抽象 -> 六款游戏并行实现 -> 性能与发布能力补齐`

这是在“六款一次性交付”目标下，仍能最大化复用与控制风险的开发路线。
