# Cocos 客户端分步实施计划（六款游戏一次性交付）

> 本文档用于“按步骤落地实现”的执行清单。目标是按 `DEV_PLAN.md` 一次性实现所有新增游戏（桃江麻将、红中麻将、长沙麻将、跑得快、益阳歪胡子、沅江千分），并补齐横屏适配、版本更新、资源分层、音效/特效/震动、日志上报与网络层改造。

---

## 0. 执行约定

- 代码改动以 `client-cocos` 为范围
- 六款游戏并行推进，但所有“共性底座”必须先落地并对六款共享
- 每一步都有“完成标准”，完成后才进入下一步

---

## 1. Step 1：横屏适配系统（AppLayoutAdapter）

### 1.1 开发任务

- 设计分辨率统一为 `1920x1080`，最小适配 `1280x720`
- 实现 `AppLayoutAdapter`：
  - 启动时读取分辨率、DPR、方向、安全区
  - 计算 `layoutScale` 与安全区 `safeLeft/right/top/bottom`
  - 屏幕尺寸变化、前后台切换时自动重算
- 在应用启动时自动创建并常驻（无需手动挂场景）

### 1.2 完成标准

- 在任意设备尺寸变化时，`AppLayoutAdapter.Snapshot` 能输出稳定数据
- 不影响现有掼蛋流程

---

## 2. Step 2：资源分层加载与包结构（Bundle + manifest）

### 2.1 开发任务

- 整理资源目录为四层概念：
  - `app-common`
  - `games/*`
  - `hotfix`
  - `version-manifest`
- 定义并实现 `manifest.json` 规范（至少包含：path、size、hash）
- 扩展资源加载器：
  - 支持按游戏加载资源包
  - 支持加载前校验（hash/版本号）
  - 支持失败重试与错误提示

### 2.2 完成标准

- 可以单独加载某一个游戏的“入口 + 加载页 + 房间/牌桌基础资源”
- 资源加载失败时可提示并可重试

---

## 3. Step 3：版本更新系统（版本检测/强更/热更）

### 3.1 开发任务

- 启动时版本检测：
  - 请求 `POST /api/app/version/check`
  - 携带 `app_version/hotfix_version/res_version` 与设备信息
  - 分支处理：强更 / 热更 / 无更新
- 热更新流程：
  - 下载（断点续传）
  - 校验（MD5/SHA256）
  - 解压与替换
  - 更新本地版本号
- 进入游戏前更新：
  - 游戏资源版本/规则版本/插件版本检查
  - 不一致则更新后再进入

### 3.2 完成标准

- 启动与进入游戏前均能正确触发更新流程
- 更新失败可重试或返回大厅

---

## 4. Step 4：网络层改造（兼容旧 MsgXXX 与新事件协议）

### 4.1 开发任务

- 实现 WS 事件层：
  - 客户端发送：`room.join / room.ready / game.action / game.chat / room.dissolve_vote / heartbeat`
  - 服务端推送：`room.update / game.start / game.deal / game.turn / game.action_result / game.settlement / room.final_settlement / user.reconnect / risk.notice`
- 兼容策略：
  - 旧协议（`MsgXXX`）与新协议（事件）通过适配层统一为同一套客户端事件
  - 现有掼蛋仍可继续跑通

### 4.2 完成标准

- 在不改服务端的情况下，旧掼蛋链路不回退
- 新游戏可以走事件协议（或至少具备切换能力）

---

## 5. Step 5：通用房间生命周期与 UI 骨架（六款共享）

### 5.1 开发任务

- 统一六款游戏的流程壳层：
  - 入口卡片 → 专属加载页 → 创建房间 → 等待房间 → 横屏牌桌 → 单局结算 → 总结算 → 回放入口
- 建立通用组件：
  - 顶部状态栏（房号/局数/玩法/网络/电量/时间/设置）
  - 玩家位视图（头像/昵称/状态/语音）
  - 聊天与表情（左下角）
  - 操作按钮区（右下安全区）
  - 断线重连提示
  - 解散投票 UI
  - 托管 UI

### 5.2 完成标准

- 六款游戏都能复用同一套壳层组件
- 任何一款游戏进入牌桌都能显示：顶部栏、玩家位、聊天入口、操作区占位

---

## 6. Step 6：六款游戏并行实现清单（按模块拆分）

> 此步骤按“六款同时推进”执行，每个游戏都按同样的子步骤完成：入口 → 加载页 → 创建/加入 → 等待房间 → 牌桌 → 单局结算 → 总结算 → 回放入口。

### 6.1 桃江麻将（2 人）

- 规则与房间：
  - 2 人 8 局 / 2 人 1 局 / 2 人 8 局对战模式
  - 房费方式（房主/AA/大赢家）
- 牌桌操作：
  - 吃（可选）、碰、杠、胡、过、托管、解散投票
  - 赖子牌标识
- 结算：
  - 单局结算、总结算

### 6.2 红中麻将

- 规则与表现：
  - 红中赖子玩法
  - 听牌提示面板
  - 胡牌牌型展示（普通/七对/碰碰胡/赖子替代）
  - 多赖子最优组合提示

### 6.3 长沙麻将

- 规则与表现：
  - 起手胡检测弹窗（缺一色/板板胡/大四喜/六六顺/节节高/三同/一枝花）
  - 中鸟动画与结算展示
  - 庄家标识、番数展示

### 6.4 跑得快（2 人 15 张）

- 牌桌交互：
  - 手牌扇形展开、选牌高亮
  - 出牌/提示/不要按钮
- 校验与托管：
  - 非法牌型提示
  - 托管（自动出牌 + 倒计时）
  - 断线重连恢复
- 牌型覆盖：
  - 单张/对子/三带/顺子/连对/飞机/炸弹/王炸

### 6.5 益阳歪胡子（3 人）

- 牌桌交互：
  - 字牌竖排/分组展示
  - 吃碰偎跑提区域固定
- 操作按钮：
  - 吃、碰、偎、跑、提、胡、过
- 胡息系统：
  - 胡息面板实时更新
  - 吃牌多组合选择弹窗

### 6.6 沅江千分（3-5 人）

- 阶段 UI：
  - 叫分/抢庄弹窗（大按钮 + 倒计时）
- 计分体系：
  - 计分牌高亮（5/10/K）
  - 累计积分面板（本轮分/累计分/目标分/庄家）
  - 目标分达标提示

### 6.7 完成标准（单游戏）

- 入口卡片可点击进入
- 专属加载页可显示进度且可失败重试
- 可创建房间、可加入房间、可进入等待房间
- 可进入牌桌并完成一局到结算
- 可出总结算并提供回放入口

---

## 7. Step 7：音效系统（全量）

### 7.1 开发任务

- 音频引擎能力：
  - 多音轨、预加载、短音效低延迟、BGM 淡入淡出、音效池化、后台暂停/恢复
- 音量层级默认值：
  - BGM 35%、环境 25%、操作 SFX 80%、强反馈 100%、语音播报 90%、金币奖励 90%
- 音效覆盖：
  - 麻将（桃江/红中/长沙）：洗牌、码牌、发牌、摸牌、出牌、吃、碰、杠、听、胡、自摸、海底捞月/大牌型 + 语音词条
  - 扑克（跑得快/千分）：洗牌、发牌、选牌、出单张、出顺子、出飞机、炸弹、王炸、结算胜利
  - 字牌（歪胡子）：发牌、出牌、吃、碰、偎、跑、提、胡

### 7.2 完成标准

- 六款游戏核心动作都有音效映射
- 设置中可独立控制音效/BGM/语音播报

---

## 8. Step 8：特效系统 + 震动反馈 + 设置扩展

### 8.1 特效系统

- 特效池化
- 金币动效 L1-L5
- 特效性能分级与自动降级规则

### 8.2 震动反馈

- 按钮点击、出牌、碰/吃、杠/炸弹、胡/自摸/大赢家、倒计时最后 3 秒、断线

### 8.3 设置扩展

- 震动开关、音效开关、BGM 开关、语音播报开关
- 特效强度（低/中/高/极致）
- 低性能模式

### 8.4 完成标准

- 设置项可持久化并对六款游戏生效
- 低端机可降级且不影响关键操作

---

## 9. Step 9：崩溃日志与性能上报

### 9.1 开发任务

- 客户端采集：
  - 崩溃/卡死
  - 资源加载失败
  - 热更新失败
  - 场景进入失败
  - WS 断线重连
  - 结算异常
  - 设备信息（机型/系统/版本/分辨率/安全区）
- 上报 API：
  - `POST /api/app/version/report`
  - `POST /api/app/resource-report`

### 9.2 完成标准

- 关键异常可被采集并上报
- 性能数据可用于自动降级策略

---

## 10. Step 10：验收清单（最终）

- 六款游戏均满足独立入口、加载页、牌桌、单局结算、总结算、回放入口
- 横屏 1920×1080 设计，适配 16:9 到 21:9，并遵守安全区规范
- 每款游戏至少 20 个核心音效
- 爆金币 L1-L5 可配置
- 低端 Android 保持 30 FPS
- 支持版本检测、热更新、强制更新，且更新过程中不破坏房间状态
- 音效/特效/震动可独立开关

